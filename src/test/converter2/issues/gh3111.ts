export class C {
    constructor(
        /** @hidden */
        readonly x: number,
        readonly y: boolean,
    ) {}

    method(
        /** @hidden */
        internalHelper?: string,
    ) {}
}
