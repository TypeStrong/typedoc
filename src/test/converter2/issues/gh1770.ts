const api = (): {
    sym1: () => number;
    sym2: number;
} => {
    return null!;
};

export const {
    /**
     * Docs for Sym1
     * @function
     */
    sym1,

    /**
     * Docs for Sym2
     */
    sym2,
} = api();
