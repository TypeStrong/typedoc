{
    "compilerOptions": {
        "strict": true,
        "module": "CommonJS",
        "allowJs": true,
        "outDir": "dist",
        "target": "ESNext",

        "noImplicitAny": false,

        // See #1524. We might force this to false eventually.
        "skipLibCheck": true
    },
    "typedocOptions": {
        "exclude": ["**/ignored.ts"]
    }
}
