/**
 * This is a comment containing a multiline code block
 * ```ts
 * export function multiply(a: number, b: number) {
 *   return a * b;
 * }
 * ```
 * @module
 */

export function multiply(a: number, b: number) {
    return a * b;
}
