<section class="tsd-member {{cssClasses}}">
    <a name="{{anchor}}" class="anchor"></a>
    {{#if name}}
        <h3>{{#each flagsArray}}<span class="tsd-flag ts-flag{{this}}">{{this}}</span> {{/each}}{{{wbr name}}}</h3>
    {{/if}}

    {{#if signatures}}
        {{> member.signatures}}
    {{else}}
        {{> member.declaration}}
    {{/if}}

    {{#each groups}}
        {{#each children}}
            {{#unless hasOwnDocument}}
                {{> member}}
            {{/unless}}
        {{/each}}
    {{/each}}
</section>