<section class="tsd-member-index">
    <h2>Index</h2>
    {{#each groups}}
        <section class="tsd-member-index-group {{cssClasses}}">
            <h3>{{title}}</h3>
            <ul class="tsd-member-index">
                {{#each children}}
                <li class="{{cssClasses}}"><a href="{{relativeURL url}}" class="tsd-kind-icon">{{#if name}}{{{wbr name}}}{{else}}<em>{{{wbr kindString}}}</em>{{/if}}</a></li>
                {{/each}}
            </ul>
        </section>
    {{/each}}
</section>